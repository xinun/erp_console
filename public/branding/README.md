# ERP Console 아이콘 원본

Chrome 탭 아이콘을 교체할 원본 이미지를 이 폴더에 `app-icon.png`라는 이름으로 넣습니다.

권장 형식:

- PNG
- 512 × 512px 정사각형
- 투명 배경 권장
- RGB 또는 RGBA 색상
- 작은 크기에서도 구분되는 단순한 심볼
- 이미지 가장자리에서 약 10% 이상의 여백

원본을 추가한 뒤 `src/app/favicon.ico`와 필요한 웹 아이콘으로 변환합니다. 원본 이미지 자체는 비밀정보를 포함하지 않아야 합니다.
